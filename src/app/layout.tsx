import type { Metadata } from "next";
import { Epilogue } from "next/font/google";
import "./globals.css";
import { TripProvider } from "./context/tripContext";
import LayoutAuth from "./components/ui/LayoutAuth"; // import client wrapper
import { Toaster } from "react-hot-toast";
import { IMAGES_ASSETS } from "./constants/ImageConstant";

const epilogue = Epilogue({
  variable: "--font-epilogue",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "eCharter",
  description: "Generated by create next app",
  icons: {
    icon: IMAGES_ASSETS.LOGO,
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${epilogue.variable} antialiased`}>
        <Toaster 
            position="top-center"
            toastOptions={{
              duration: 3000,
            }}
          />
        <TripProvider>
          <LayoutAuth>{children}</LayoutAuth>
        </TripProvider>
      </body>
    </html>
  );
}
